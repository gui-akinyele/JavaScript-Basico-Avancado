// Nomes de variáveis reservadas

// Alguns nomes não podem ser utilizados para iniciar variáveis, veja:
// break, case, catch, class, const, continue, debugger, default, delete, do, else, enum, export, extends
// false, finally, for, function, if, implements, import, interface, in, instanceof, let, new, package, private,
// protected, public, return, static, super, switch, this, throw, true, try, typeof, var, void, while, with, yield

// let break = 'parar'; erro, palavra reservada
// let if = 'Teste'; erro, palavra reservada
// let function = 'Teste'; erro, palavra reservada

// Porém podemos usar elas + alguam palavra;dígito:

let functionTeste = 'teste';
let function1 = 'teste1';
let breakGui = 1;

console.log(functionTeste);
console.log(function1);
console.log(breakGui);



