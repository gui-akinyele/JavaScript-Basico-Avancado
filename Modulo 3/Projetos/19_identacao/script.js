// Identação

// Serve par organizar o código e deixar mais legível;
// Não há uma regra, porém cada bloco aninhando deve ser identado uma vez;

let a = 2;
let b = 0;

if(a > 2) {
    b = x + 2;
    if(b == 4) {
        console.log('B é 4');
    }
}

let x = 0;
let y = 0;

if(x == 0 && y == 0) {
    let z = 5;
    if(z > 2 ) {
        console.log(z);
        for(i = 0; i < 5; i = i + 1) {
            console.log(i);
            if(i == 2) {
                console.log("i é = 2");
            }
        }
    }

}
