// * Crie uma constante com o seu nome como valor;
// * Depois uma esrtrutura if que verifica se o seu nome é o que está na constante;
// ! Se tiver, emita uma mensagem de saudação com o console.log();

const nome = "Guilherme";

if ( nome == "Guilherme") {
    console.log(`Olá ${nome}, bem vindo!`);
}